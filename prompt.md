Build me an MCP server that can connect to azure devops to run WIQL commands that I provide in the service layer for an AI harness that supports MCP clients to use. 

It needs to be deployable as an NPM package than can run locally or be deployed to NPM to be used by npx

It needs to pass through my authentication, not store them.

Write it in javascript using the approprotate  frameworks and libraries needed. 

Keep the code modular and well commented.

There is no need for external storage.